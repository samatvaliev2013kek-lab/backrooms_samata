name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: barichello/godot-ci:4.4.1
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Export APK
        run: |
          mkdir -p build
          godot --export-release "Android" "build/game.apk"
          ls -la build/
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: game-apk
          path: build/*.apk
